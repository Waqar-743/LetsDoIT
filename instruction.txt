Current problems:

Teacher creates a course, but student cannot join using the 4-digit code.
AI responses are dummy/fake.
Local model download/path setup is incomplete.
Online Gemma mode is not properly configured.
Teacher cannot properly upload PDFs/material.
Student cannot chat with the model in real time.
Features are not connected to real database/backend state.
Must Fix
Course Flow

Teacher creates course → app stores 4-digit code → student enters code → joins course → course appears in student dashboard → teacher sees enrolled student.

Use real database persistence, not dummy frontend state.

Material Upload

Teacher can upload PDFs, slides, notes, and images inside a course.

Students can view/download uploaded material and send it to AI chat.

Real Gemma Integration

Remove dummy model responses.

Implement:

Offline Mode: local Gemma through Ollama / llama.cpp / mistral.rs
Online Mode: hosted Gemma through OpenRouter
Auto Mode: use online if available, otherwise fallback to local

Use provider structure:

LocalGemmaProvider
OpenRouterGemmaProvider
AutoGemmaRouter
OpenRouter Setup

Use Gemma, not Gemini.

Base URL: https://openrouter.ai/api/v1
Endpoint: /chat/completions
Default model: google/gemma-4-26b-a4b-it:free
Backup model: google/gemma-4-31b-it:free

Add settings for:

OpenRouter API key
Model ID
Test connection button
Local Model Setup

Support Ollama first:

Endpoint: http://localhost:11434/api/chat
Model: user-selected Gemma model

Add settings for:

local endpoint
model name/path
test local connection
model status
Student AI Chat

Student dashboard must have real AI chat with:

Offline / Online / Auto mode
real model response
course material context
summary, explanation, quiz/practice generation
Final Test Flow

Teacher logs in → creates course → uploads PDF → student joins with code → sees material → sends material to AI → chats with real Gemma → teacher sees student activity.

Do not add more dummy UI. Fix real backend, database, file upload, course join, and Gemma integration.